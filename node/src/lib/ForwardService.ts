import { Config } from '../config/Config'

import { InitialMessage, ForwardMessage } from '../../../common/lib/messages'

export class ForwardService {
    constructor(private config: Config) {}

    forwardInitial = (message: InitialMessage) => {
        
    }
}